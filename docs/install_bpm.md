# Install BPM

Pick one of the following approaches.

## Option A: pipx (recommended for CLI tools)
```
pipx install bpm-cli
```

## Option B: pip (user install)
```
python -m pip install --user bpm-cli
```

## Verify
```
bpm --help
```

If you are installing from a specific repo or tag, follow the BPM project README.
