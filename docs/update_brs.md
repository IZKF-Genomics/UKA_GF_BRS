# Update this BRS

## If installed from Git
```
bpm resource update UKA_GF_BRS
```

## If using a local checkout
```
cd /path/to/UKA_GF_BRS
git pull
```

Then re-run `bpm template list` to confirm updates are visible.
