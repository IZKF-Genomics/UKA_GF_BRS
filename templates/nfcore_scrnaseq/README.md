# nfcore_scrnaseq


<!-- AGENT_METADATA_START -->
## Agent Metadata
```yaml
id: nfcore_scrnaseq
kind: template
description: nf-core/scrnaseq wrapper; requires genome and Cell Ranger index. Samplesheet
  generated by post_render hook.
descriptor: templates/nfcore_scrnaseq/template_config.yaml
required_params:
- agendo_id
- cellranger_index
- genome
optional_params:
- aligner
- expected_cells
- max_cpus
- max_memory
- protocol
- skip_cellbender
cli_flags:
  genome: --genome
  agendo_id: --agendo-id
  cellranger_index: --cellranger_index
  aligner: --aligner
  protocol: --protocol
  skip_cellbender: --skip_cellbender
  max_cpus: --max-cpus
  max_memory: --max-memory
run_entry: run.sh
publish_keys:
- nfcore_multiqc
- nfcore_samplesheet
render_file_count: 2
```
<!-- AGENT_METADATA_END -->

nf-core/scrnaseq wrapper for single-cell RNA-seq data (default aligner: Cell Ranger).

## Render
Run from the project directory (where project.yaml lives).
```
bpm template render nfcore_scrnaseq \
  --param genome=GRCh38 \
  --param agendo_id=12345 \
  --param cellranger_index=/data/shared_env/10xGenomics/refdata-gex-GRCh38-2020-A
```

## Run
```
bpm template run nfcore_scrnaseq --dir /path/to/project
```

## Parameters
See `template_config.yaml` for the full parameter list and defaults.
