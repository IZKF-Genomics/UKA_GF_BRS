id: illumina_methylation_compare
description: "Illumina methylation array multi-comparison analysis (DMP/DMR/enrichment/drilldown) from preprocessed inputs."
required_templates:
  - illumina_methylation_process

params:
  study_name:
    type: str
    cli: "--study-name"
    required: false
    default: ""
    description: "Optional project/report name; defaults to rendered template id when omitted"
  authors:
    type: str
    cli: "--authors"
    required: false
    default: ""
    description: "Author name shown in rendered reports"
  array_type:
    type: str
    cli: "--array-type"
    required: false
    default: "AUTO"
    description: "Array type for enrichment mapping. Use AUTO for mixed 450K/EPIC inputs."
  genome_build:
    type: str
    cli: "--genome-build"
    required: false
    default: "hg38"
    description: "Genome build used for annotation and reporting"
  group_col:
    type: str
    cli: "--group-col"
    required: false
    default: "group"
    description: "Primary grouping column in config/samples.csv"
  auto_discover_inputs:
    type: bool
    cli: "--auto-discover-inputs"
    required: false
    default: true
    description: "Auto-populate config/input_registry.csv from active illumina_methylation_process runs in project.yaml"

render:
  into: "${ctx.project_dir}/${ctx.template.id}/"
  files:
    - README.md -> README.md
    - pixi.toml -> pixi.toml
    - run.sh.j2 -> run.sh
    - .gitignore -> .gitignore
    - config/project.toml.j2 -> config/project.toml
    - config/input_registry.csv.j2 -> config/input_registry.csv
    - config/comparisons.csv -> config/comparisons.csv
    - config/group_map.csv -> config/group_map.csv
    - config/samples.csv -> config/samples.csv
    - config/samples_paths.csv -> config/samples_paths.csv
    - common.R -> common.R
    - scripts/sync_samples_from_inputs.R -> scripts/sync_samples_from_inputs.R
    - 01_dmp.qmd.j2 -> 01_dmp.qmd
    - 02_dmr.qmd.j2 -> 02_dmr.qmd
    - 03_enrichment.qmd.j2 -> 03_enrichment.qmd
    - 04_drilldown.qmd.j2 -> 04_drilldown.qmd
    - 00_index.qmd.j2 -> 00_index.qmd

run:
  entry: "run.sh"

hooks:
  post_render:
    - hooks.compare_autodiscover_registry:main
