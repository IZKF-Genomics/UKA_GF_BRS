Downstream methylation comparison analyses were performed on preprocessed methylation datasets generated by upstream BPM runs.
The comparison template aggregated selected input runs, harmonized sample metadata, and executed differential and enrichment-oriented analyses as configured.
Result tables and figures were produced in a reproducible report structure with explicit provenance to input runs and parameters.
